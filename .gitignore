.DS_Store
strategy/

# Screenshot tooling artifacts (not part of the prototype)
node_modules/
package.json
package-lock.json
chorus-screenshots.zip

# ...but the CAD port's demo is real source and needs its manifest.
!port/demo/package.json
!port/demo/package-lock.json
